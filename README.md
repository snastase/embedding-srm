# Aligning contextual embeddings with SRM
This repo contains a brief Jupyter tutorial on aligning contextual embeddings using the orthogonal Procrustes transformation and shared response model (SRM).
